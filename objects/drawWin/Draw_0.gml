/// @description Insert description here
// You can write your code in this editor

if global.hasNPCRoboBlor draw_sprite(sprVictory4, image_index, x, y)
if global.hasNPCTrimblina draw_sprite(sprVictory3, image_index, x, y)
if global.hasNPCPintendo draw_sprite(sprVictory2, image_index, x, y)

draw_self()
